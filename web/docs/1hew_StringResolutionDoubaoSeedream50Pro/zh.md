# String Resolution Doubao Seedream 5.0 Pro - 豆包 Seedream 5.0 Pro 分辨率选择器

**节点功能：** `String Resolution Doubao Seedream 5.0 Pro` 根据输入图像面积输出最接近的 Doubao Seedream 5.0 Pro 分辨率档位标签（`1k` / `1.5k` / `2k`）；如果未连接图像，则直接输出手动选择的标签。与 Doubao Seedream 5.0 Pro 成对使用。

## 输入

| 参数名称 | 入端选择 | 数据类型 | 默认值 | 取值范围 | 描述 |
| -------- | -------- | -------- | ------ | -------- | ---- |
| `selection` | - | COMBO | `1k` | `1k` / `1.5k` / `2k` | 手动选择的 Doubao Seedream 5.0 Pro 分辨率标签；未连接 `image` 时直接输出此值 |
| `image` | 可选 | IMAGE | - | - | 输入图像批次，用于逐张推断最接近的分辨率档位 |

## 输出

| 输出名称 | 数据类型 | 描述 |
|---------|----------|------|
| `string` | STRING | 分辨率标签字符串；批量输入时按批次顺序以换行拼接输出 |

## 档位与目标面积

| 档位 | 1:1 官方尺寸 | 目标面积（像素） |
| ---- | ------------ | ---------------- |
| `1k` | `1024x1024` | 1,048,576 |
| `1.5k` | `1536x1536` | 2,359,296 |
| `2k` | `2048x2048` | 4,194,304 |

## 功能说明

- Doubao Seedream 5.0 Pro 档位：`1k` / `1.5k` / `2k`，按各档 1:1 官方尺寸的面积作为目标值。
- 面积匹配：按图像总像素面积在各档位目标面积中选择最近值（对数距离）。
- 手动回退：未连接图像时直接透传手动选择的标签。
- 支持批处理：图像批次中的每张图都会输出一行分辨率标签。

## 典型用法

- 在需要分辨率字符串参数的 Doubao Seedream 5.0 Pro 节点前，把图像尺寸转换成分辨率标签，接到 API 节点的 `resolution` 入端。
- 不连接 `image` 时，可直接当作固定分辨率字符串选择器使用。

## 注意与建议

- 匹配依据是总像素面积，不考虑具体宽高比。
- 目标面积逐项对应 1hewOffice 的 `DoubaoSeedream50Pro.SIZE_MAP` 中每档的 1:1 官方尺寸；该来源属于 1hewOffice，其源码不在本仓库内。
- 该节点只输出字符串标签，不会修改图像尺寸。
