# Image Resize GPT Image 2.0 - GPT Image 2.0 尺寸适配

**节点功能：** `Image Resize GPT Image 2.0` 用于将输入图像/遮罩调整到 GPT Image 2.0 可接受的尺寸。`auto` 会先按输入面积确定 `1k`/`2k`/`4k` 档位、再在该档内选最接近比例的预设（`auto (1k)` 等则固定档位）；`dynamic*` 则按 GPT Image 2.0 规则尽量保持输入比例并动态缩放。

## 输入

| 参数名称 | 入端选择 | 数据类型 | 默认值 | 取值范围 | 描述 |
| -------- | -------- | -------- | ------ | -------- | ---- |
| `preset_size` | - | COMBO | `auto (2k)` | `auto*` / `dynamic*` / 各预设尺寸项 | 目标尺寸选择；`auto` 按输入面积定档后选最近比例，`auto (1k)` 等固定档位，`dynamic*` 保持输入比例动态缩放，具体预设项输出固定尺寸 |
| `fit` | - | COMBO | `crop` | `crop` / `pad` / `stretch` | 适应模式：裁剪、填充、拉伸 |
| `pad_color` | - | STRING | `1.0` | 灰度/HEX/RGB/颜色名/`edge`/`average`/`extend`/`mirror` | `pad` 模式背景填充策略 |
| `image` | 可选 | IMAGE | - | - | 输入图像批次 |
| `mask` | 可选 | MASK | - | - | 输入遮罩批次，可与图像同步变换 |

## 输出

| 输出名称 | 数据类型 | 描述 |
|---------|----------|------|
| `image` | IMAGE | 调整后的图像批次 |
| `mask` | MASK | 与输出尺寸对齐的遮罩批次 |
| `native_image` | IMAGE | 主输出的原生分辨率副本；内容、裁剪与填充一致，仅分辨率不同 |
| `native_mask` | MASK | 与 `native_image` 对齐的遮罩 |

## 功能说明

- 预设 auto：`auto` 会先按输入面积确定 `1k` / `2k` / `4k` 档位，再在该档内匹配最接近比例的尺寸；`auto (1k)` / `auto (2k)` / `auto (4k)` 则固定在该档内匹配比例。
- 动态 dynamic：`dynamic` 会按输入面积选择最接近的 `1k` / `2k` / `4k` 档位并保持输入比例动态缩放；`dynamic (1k)` / `dynamic (2k)` / `dynamic (4k)` 会固定档位。
- 固定预设：提供 `1k`、`2k`、`4k` 三档下常用比例的稳定尺寸，适合需要明确输出尺寸的工作流。
- GPT Image 2.0 规则：动态尺寸与预设尺寸都会按 16 像素对齐、安全比例范围与最大边 3840 等规则处理。
- 支持无图像或无遮罩场景：只输入 `image` 或 `mask` 也可执行；都为空时输出目标尺寸背景图与全白遮罩。
- 三种适配模式：
  - `crop`：先按比例中心裁剪再缩放到目标尺寸。
  - `pad`：先按比例缩放再居中填充到目标尺寸。
  - `stretch`：直接拉伸到目标尺寸。

## 典型用法

- 送入 GPT Image 2.0 编辑前对齐到常见预设：`preset_size=auto (2k)`，`fit=crop`。
- 尽量保持输入原始比例：`preset_size=dynamic (2k)`，`fit=pad` 或 `fit=crop`。
- 需要固定横图尺寸：选择 `[2k] 2560x1440 (16:9)` 或 `[4k] 3840x2160 (16:9)`。
- 保留完整画面并补边：`fit=pad`，按需要设置 `pad_color`（如 `1.0`、`#000000`、`mirror`）。
- 只用遮罩生成对应画布：只连接 `mask` 输入，节点会生成对应背景图并输出对齐后的遮罩。

## 固定预设尺寸表

| 比例 | 1k 尺寸 | 2k 尺寸 | 4k 尺寸 |
| ---- | ------ | ------ | ------ |
| `3:1` | `1728x576` | `3456x1152` | `3840x1280` |
| `21:9` | `1344x576` | `2688x1152` | `3808x1632` |
| `2:1` | `1440x720` | `2880x1440` | `3840x1920` |
| `16:9` | `1280x720` | `2560x1440` | `3840x2160` |
| `3:2` | `1248x832` | `2496x1664` | `3504x2336` |
| `4:3` | `1152x864` | `2304x1728` | `3264x2448` |
| `5:4` | `1120x896` | `2240x1792` | `3200x2560` |
| `1:1` | `1024x1024` | `2048x2048` | `2880x2880` |
| `4:5` | `896x1120` | `1792x2240` | `2560x3200` |
| `3:4` | `864x1152` | `1728x2304` | `2448x3264` |
| `2:3` | `832x1248` | `1664x2496` | `2336x3504` |
| `9:16` | `720x1280` | `1440x2560` | `2160x3840` |
| `1:2` | `720x1440` | `1440x2880` | `1920x3840` |
| `9:21` | `576x1344` | `1152x2688` | `1632x3808` |
| `1:3` | `576x1728` | `1152x3456` | `1280x3840` |

## 注意与建议

- 这张表是按 GPT Image 2.0 尺寸规则归纳出的节点固定预设，不是官方固定枚举表。
- 三档尺寸均**严格等于名义比例**：`1k` / `2k` 由 `(a×k, b×k)` 构造（`k` 为 16 的倍数）；`4k` 在官方约束**与本节点自身 `3840` 最长边上限**下取**最大精确值**。
- 官方自定义尺寸三条硬规则（`GPT Image 2`）：宽高均为 **16 的倍数**、宽高比 **≤ 3:1**、总像素 **655,360 ~ 8,294,400**。本表全部满足；`3840` 最长边是节点自身的额外限制（不属于官方三条规则），正是它让 `4k` 的 `2:1` 档停在 `3840x1920` 而不再变大。
- `auto` 会先按输入面积定档，再从对应档的表里挑最近比例；`auto (1k)` 等固定档位；`dynamic*` 会按输入比例动态计算。
- `4k` 尺寸生成可能耗时较长，下游 API 节点建议提高 `timeout_sec`。
